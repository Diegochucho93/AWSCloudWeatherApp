# PostgreSQL Database Configuration
DB_USER=postgres
DB_HOST=localhost
DB_NAME=weather_db
DB_PASSWORD=postgres
DB_PORT=5432
DB_SSL=false

# For AWS RDS or other cloud databases that require SSL, set DB_SSL=true
# DB_SSL=true

# Note: No API key needed! Using weather.gov (National Weather Service) free API
# This API provides data for US locations only
